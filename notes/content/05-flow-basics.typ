#import "@preview/theorion:0.4.1": *
#import "@preview/tablem:0.3.0": three-line-table

= 第五章 网络流基础

#tip-box(title: "这一章的主线")[
  网络流把“资源从哪里来、能经过哪里、最多能送多少、哪些点只负责转运”统一写成容量约束和守恒约束。
  后面的匹配、割、项目选择、体育淘汰等问题都会反复使用这套建模语言。
]

== 网络流：什么是流

#definition[
  在流网络中，图是有向的，有源点 $s$ 和汇点 $t$，每条边有容量 $c(e) >= 0$。
  一个 $s$-$t$ 流 $f$ 需要满足：

  - 容量约束：`0 <= f(e) <= c(e)`；
  - 守恒约束：除 $s, t$ 外，每个点流入量等于流出量。
]

最大流问题的目标是：让从 $s$ 送到 $t$ 的总流量最大。

这套定义非常重要，因为后面很多看似无关的问题都会被改写成：

- 边表示“可分配的关系”；
- 容量表示“最多能分多少”；
- 守恒表示“中间点只负责转运，不负责凭空制造或消灭资源”。

== 为什么朴素贪心不行

一个最容易想到的算法是：

- 从全零流开始；
- 找一条还能继续送流的 $s$-$t$ 路径；
- 沿路尽量多送；
- 重复直到送不动。

但这里必须记住：*locally optimality != globally optimality*。

原因是你早期走过的路径，可能把某些关键边占住了；如果没有“撤销”机制，算法可能卡在一个不是最优的流上。

== 残量网络：允许你后悔

Ford-Fulkerson 的关键发明就是 *residual graph*。

- 对原边 $(u, v)$，若还有剩余容量，则在残量图中保留正向边；
- 若这条边已经送过流，则加入反向边 $(v, u)$，表示你可以撤回一部分之前送出的流。

#definition[
  残量图中的一条 $s$-$t$ 路径叫做 augmenting path。
  它的瓶颈容量是路径上最小的残量容量。
]

沿着增广路送流，相当于：

- 在某些边上继续加流；
- 在另一些边上撤回旧流再重分配。

这一步让算法从“不会回头的贪心”变成了“可修正的迭代改进”。

== Ford-Fulkerson 算法框架

算法可以概括为：

1. 初始化 `f(e) = 0`；
2. 在残量图中找一条增广路；
3. 按瓶颈容量增广；
4. 若不存在增广路，则停止。

本章只先建立这个框架。真正支撑它正确性的最大流最小割定理，要到下一章系统证明。

#tip-box(title: "先记住两个关键词")[
  - *残量图*：表示“当前还有哪些调整空间”。
  - *增广路*：表示“当前还能把总流量继续往上推的一条证据”。
]

== 本章小结

#figure(
  caption: [网络流基础概念],
  three-line-table[
    | 概念 | 含义 | 作用 |
    |:---|:---|:---|
    | 容量约束 | 每条边最多能送多少 | 限制局部资源 |
    | 守恒约束 | 中间点流入等于流出 | 保证流不会凭空产生或消失 |
    | 残量图 | 当前流还能如何调整 | 支持撤回和重新分配 |
    | 增广路 | 残量图中的 $s$-$t$ 路径 | 证明当前流还能变大 |
  ],
)

#tip-box(title: "你现在应当能说清")[
  - 残量图为什么是最大流算法必不可少的结构。
  - 增广路为什么等价于“当前流还没推到极限”。
  - Ford-Fulkerson 为什么需要允许撤回旧流。
]
