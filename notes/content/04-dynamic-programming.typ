#import "@local/notes:0.1.0": *

= 第四章 动态规划

#tip-block(title: "动态规划的起点")[
  如果一个问题直接递归会反复解同一个子问题，那么就该警觉：它可能不是分治，而是动态规划。
]

== 动态规划到底和分治差在哪

先把三种范式放在一起比较：
- 贪心：每一步做一个不可回退的局部选择；
- 分治：把问题拆成若干 *相互独立* 的子问题；
- 动态规划：把问题拆成许多 *重叠* 的子问题，并按规模从小到大复用答案。

所以判断 DP 的两个信号是：
1. 存在最优子结构；
2. 子问题会大量重复出现。

== 带权区间调度：DP 的标准入门题

#definition[
  *Weighted Interval Scheduling*：每个作业 $j$ 除了开始时间 $s_j$ 和结束时间 $f_j$，还有价值 $v_j$。目标是选择一组两两兼容的作业，使总价值最大。
]

如果所有权重都相等，普通区间调度的贪心规则就够了；一旦权重任意，贪心可能惨败。于是我们重新设计状态。

先按结束时间排序，使 `f_1 <= f_2 <= ... <= f_n`。

再定义：
- `p(j)`：在 $j$ 之前、且与 $j$ 兼容的最后一个作业下标；
- `OPT(j)`：只看前 $j$ 个作业时，能够得到的最优总价值。

对作业 $j$，只有两个选择：

- 选它：价值为 `v_j + OPT(p(j))`；
- 不选它：价值为 `OPT(j-1)`。

因此得到经典递推：

`OPT(j) = max(v_j + OPT(p(j)), OPT(j-1))`。

#important-block(title: "为什么这是 DP，而不是贪心")[
  因为对于作业 $j$，你无法只凭某个局部标准决定“选还是不选”。
  真正决定因素是：选它以后，左边还能接出多大价值；不选它时，前缀最优又是多少。
]

实现上有两种常见方式：

- 记忆化搜索：自顶向下写递归，把算过的子问题存起来；
- 自底向上：按 `j = 1, 2, ..., n` 直接填表。

时间复杂度通常是 `O(n log n)`：排序 `O(n log n)`，求所有 `p(j)` 也可以在排序后高效完成。

== 背包问题：给状态再加一个维度

#definition[
  *0/1 Knapsack*：每件物品 $i$ 有重量 $w_i$ 和价值 $v_i$，背包容量为 $W$。
  每件物品要么选一次，要么不选。目标是在总重量不超过 $W$ 的前提下，使总价值最大。
]

若只用一个下标 $i$ 表示“看前 $i$ 件物品”，信息不够，因为剩余容量也会影响决策。所以要增加第二个维度：

`OPT(i, w)` 表示“只看前 $i$ 件物品，且容量上限为 $w$ 时的最大价值”。

递推分两种情况：

- 不选第 $i$ 件：`OPT(i-1, w)`；
- 若 $w_i <= w$，则可选第 $i$ 件，价值是 `v_i + OPT(i-1, w-w_i)`。

所以 `OPT(i, w) = max(OPT(i-1, w), v_i + OPT(i-1, w-w_i))`。

复杂度为 `Theta(nW)`。

#warning-block(title: "为什么这题不是真正意义上的多项式时间")[
  这里的复杂度依赖于容量数值 $W$，而不是输入长度 `log W`。
  因此这是一个 *伪多项式算法*。这点在复杂性分析里很重要。
]

#remark(title: "这会和后面的 NP 完全性冲突吗")[
  不会。后面学 `SUBSET-SUM` 与 `KNAPSACK` 时你会发现：
  “存在 `Theta(nW)` 的 DP” 与 “decision version 是 NP-complete” 可以同时成立。
  原因正是复杂性按输入编码长度算，而不是按数值大小本身算。
]

== RNA 二级结构：区间 DP

RNA 二级结构说明了一件事：有时状态不是“前缀”，而是“区间”。

问题约束包括：

- 只有互补碱基才能配对；
- 不能 sharp turn，即一对配对端点之间至少隔 4 个字符；
- 不能交叉配对。

若定义 `OPT(i, j)`，表示子串 $b_i b_{i+1} ... b_j$ 的最优配对数，那么当考虑末尾字符 $b_j$ 时有两种情况：

- $b_j$ 不参与配对，则值为 `OPT(i, j-1)`；
- $b_j$ 与某个 $b_t$ 配对，则分成左右两个互不交叉的子区间。

因此会得到一个“枚举分割点”的区间 DP。最终复杂度是：

- 时间 `O(n^3)`；
- 空间 `O(n^2)`。

#tip-block(title: "为什么区间 DP 常常是三次方")[
  因为状态数通常是 `O(n^2)` 个区间，而每个区间往往还要枚举一个分割点或配对点，这再乘一个 `O(n)` 就变成了 `O(n^3)`。
]

== 序列比对与编辑距离

#definition[
  *Sequence Alignment*：给定字符串 $X = x_1 ... x_m$ 与 $Y = y_1 ... y_n$，允许 mismatch 与 gap，并为它们赋予代价。目标是找到总代价最小的对齐方式。
]

这是“前缀 DP”最经典的题型之一。定义：

`OPT(i, j)`：字符串 `X` 的前 $i$ 个字符与字符串 `Y` 的前 $j$ 个字符之间的最小对齐代价。

最后一步只有三种可能：

- $x_i$ 与 $y_j$ 对齐；
- $x_i$ 与 gap 对齐；
- $y_j$ 与 gap 对齐。

于是递推自然写成三者最小值。时间和空间复杂度都是 `Theta(mn)`。

这类题最重要的不是公式，而是“最后一步分类讨论”的状态设计思路。很多二维 DP 都来自这个套路。

== 线性空间序列比对：DP 和分治结合

当 $m, n$ 很大时，`Theta(mn)` 空间可能装不下。Hirschberg 算法的核心思想是：
- 正向做一次 DP，求到中间列的最短路代价；
- 反向再做一次 DP，求从中间列到终点的最短路代价；
- 找到那条最优路径穿过中间列的位置；
- 以此为界，递归地把问题切成左右两半。

这样总时间仍然是 `O(mn)`，但空间下降到 `O(m+n)`。

#remark(title: "这是一类很重要的技巧")[
  当完整 DP 表太大时，不一定要彻底放弃动态规划。
  你可以只保留一行或一列，再结合分治恢复整条最优路径。
]

== 带负权边的最短路：为什么 Dijkstra 会失败

最短路也可以从动态规划角度重新理解。这里允许边权为负。

一旦存在负权边，Dijkstra 的“贪心地固定当前最近点”就不再安全，因为：

- 某个点现在看起来最短；
- 但之后可能经由一条负权边，把它的距离继续降低。

把所有边权统一加常数也不行，因为路径边数不同，加常数会改变不同路径之间的相对优劣。

#warning-block(title: "负权边与负环要分清")[
  允许负权边不代表问题无解；
  真正使“最短路”失去意义的是 *可达的负权环*。
  因为你可以在环里无限绕，把路径长度降到负无穷。
]

== Bellman-Ford 的 DP 视角

Bellman-Ford 可以从一个很干净的状态定义推出：

`OPT(i, v)`：从顶点 $v$ 到终点 $t$，在“最多只能走 $i$ 条边”的限制下，最短路有多短。

于是有两种情况：
- 最优路根本没用满 $i$ 条边，即值为 `OPT(i-1, v)`；
- 最优路第一条边走向某个邻居 $w$，于是值为 `c(v, w) + OPT(i-1, w)`。

所以本质上是在逐步放宽“允许使用的边数上限”。若图中不存在负环，则最短简单路径最多只需$n-1$ 条边，因此答案会在 $i = n-1$ 时稳定下来。

这就是 Bellman-Ford 的理论核心。

== Bellman-Ford 的实现与意义

标准实现的复杂度是：

- 时间 `Theta(mn)`；
- 空间可优化到 `O(n)`。

此外还有两个应用值得记：

1. *Distance Vector Protocol*：每个路由器只和邻居交换距离信息，也能逐步收敛。
2. *Negative Cycle Detection*：若第 $n$ 轮还可以继续改进，就说明存在负环。

汇率套利例子尤其值得记住。若把汇率 $r_i$ 变成边权，并按 `w_i = -log r_i` 做变换，
那么一圈乘积大于 1 的套利机会，就等价于图中存在负权环。

#important-block(title: "数学变换的意义")[
  乘积难处理，取对数后会变成求和。
  因此“是否存在一组汇率连乘超过 1”就转成了“是否存在权重和为负的环”。
]

== 动态规划部分的统一视角

到这里为止，动态规划已经不只是一类表格题，而是一种 *状态建模语言*：

- 带权区间调度：一维状态，做二选一。
- 背包：二维状态，多了容量约束。
- RNA：二维区间状态。
- 序列比对：二维前缀状态。
- Bellman-Ford：二维状态，多了“允许边数”这一维。

== 本章总结：DP 状态该怎么想

#figure(
  caption: [本章几类典型动态规划状态设计],
  three-line-table[
    | 问题 | 状态 | 关键额外维度 |
    |:---|:---|:---|
    | 带权区间调度 | `OPT(j)` | 最后一个可兼容作业 `p(j)` |
    | 背包 | `OPT(i, w)` | 容量上限 |
    | RNA 二级结构 | `OPT(i, j)` | 区间左右端点 |
    | 序列比对 | `OPT(i, j)` | 两个前缀长度 |
    | Bellman-Ford | `OPT(i, v)` | 最多允许使用的边数 |
  ],
)

#tip-block(title: "做 DP 题时的固定顺序")[
  1. 先定义状态，说明它到底代表什么。
  2. 再问最后一步有哪些互斥情况。
  3. 写出递推式。
  4. 最后才决定记忆化还是自底向上。
]
