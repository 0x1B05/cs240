#import "@local/notes:0.1.0": *

= 第九章 归约、P 与 NP

== 为什么需要“问题分类”

到前九讲为止，我们看到过很多算法范式：贪心；分治；动态规划；最大流最小割。

从这一讲开始，课程换了一个问法：如果你试了很多聪明方法，还是看不到多项式时间算法，问题可能不在技巧，而在题目本身的复杂性。于是要问：它只是我还没想到；还是它本来就很难？

复杂性理论的作用，就是把大量“看起来不同”的困难问题放进同一张地图里。

== 多项式时间归约：把一种困难翻译成另一种困难

#definition[
  若问题 $X$ 的任意实例，都能通过多项式时间的预处理，加上对问题 $Y$ 的多项式次调用来解决，则称 $X$ 多项式时间归约到 $Y$，记作 `X <=p Y`。
]

它的含义是：如果我会解 $Y$；那我也就会解 $X$。因此，归约方向必须这样：`X <=_p Y` 表示“$Y$ 至少和 $X$ 一样难”。
#example[
    X 一元一次方程, Y 一元二次方程. `X <=_p Y`, 如果我会解一元二次方程, 那么就一定可以解一元一次方程.

]


#warning-block(title: "最容易读反的地方")[
  做 NP-complete 题时，最容易犯的错就是把归约方向看反。
  你是把 *已知困难* 的问题翻译到 *目标问题* 上，
  以此说明目标问题至少同样困难。
  所以证明 `Y` 难时，通常应当从一个已知难问题 `X` 出发，证明 `X <=_p Y`。
]

== 归约的三种常见方式

这类归约大致有三种常见写法。

=== 1. 简单等价

最经典例子是 *Independent Set* 与 *Vertex Cover*。

#definition[
  *Independent Set*：给定图 $G = (V, E)$ 和整数 $k$，问是否存在大小至少为 $k$ 的点集 $S subset.eq V$，使得任意边都不会同时接在 $S$ 中两个点上。
]

#definition[
  *Vertex Cover*：给定图 $G = (V, E)$ 和整数 $k$，问是否存在大小至多为 $k$ 的点集 $C subset.eq V$，使得每条边至少有一个端点落在 $C$ 中。
]

#theorem(title: "独立集与点覆盖互为补集")[
  对任意图 $G = (V, E)$，集合 $S$ 是独立集，当且仅当 $V - S$ 是点覆盖。
]

这个例子好在它几乎不需要构造：

- 不是造一个复杂 gadget；
- 而是发现两个问题本来就在描述同一结构的两个侧面。

若某条边的两个端点都落在 $S$ 中，那么 $S$ 就不是独立集；等价地说，要想让 $S$ 成为独立集，边的至少一个端点必须落在补集 $V-S$ 中，这正是点覆盖的定义。

=== 2. 从特殊情形归约到一般情形

`Vertex Cover <=_p Set Cover` 是第二类套路的例子。

#definition[
  *Set Cover*：给定全集 $U$、若干子集 $S_1, S_2, dots, S_m subset.eq U$ 以及整数 $k$，问是否能选出不超过 $k$ 个集合，使它们的并集恰好覆盖 $U$。
]

把一张图变成 set cover 的方法很自然：

- 令全集 `U = E`，也就是“要被覆盖的对象”是图里的边；
- 对每个顶点 $v$，建立一个集合 $S_v$，其中放入所有与 $v$ 相 incident 的边。

于是：

- 选一个顶点进入 vertex cover；
- 就等价于选对应的集合去覆盖所有 incident 边。

所以点覆盖只是 set cover 的一个很特殊实例。这种归约的特点是：

- 目标问题更一般；
- 源问题只是它的一个受限版本。

=== 3. 用 gadget 编码逻辑约束

第三类套路最重要也最常见：把布尔公式的真假，编码成图或组合结构中的“可行/不可行”。

第一个需要一点 gadget 思想的例子是 `3-SAT <=_p Independent Set`。

构造思想是：

- 每个 clause 造 3 个点，分别对应 3 个 literal；
- 同一 clause 内把这 3 个点连成三角形；
- 对互相矛盾的 literal，例如 `$x$` 与 `¬x`，也连边；
- 令 `k = 子句数`。

这样一来，若想找大小为 $k$ 的独立集，就会被迫满足两件事：

- 每个三角形里至多选一个点，所以每个 clause 至多选一个 literal；
- 矛盾 literal 之间有边，所以不能同时把 `$x$` 和 `¬x` 都选进来。

而因为总共要选 $k$ 个点，又有 $k$ 个 clause，所以实际上必须“每个 clause 恰好选一个”，这正对应“给每个 clause 选一个为真的 literal”。

#important-block(title: "读 gadget 的方法")[
  看 gadget 时先问：它到底强制了什么逻辑关系？

  - 三角形强制“同一子句最多选一个”。
  - 冲突边强制“互补文字不能同时为真”。

  只要你抓住每个 gadget 的逻辑作用，就不需要死背图长什么样。
]

== 归约为何能传递“可解”与“不可解”

归约的力量来自两条最基本的推论：

- 若 `X <=_p Y` 且 $Y$ 可在多项式时间内解决，则 $X$ 也可在多项式时间内解决。
- 若 `X <=_p Y` 且已知 $X$ 不可能多项式时间解决，那么 $Y$ 也不可能。

此外，归约还具有传递性：

#theorem(title: "归约的传递性")[
  若 `X <=_p Y` 且 `Y <=_p Z`，则 `X <=_p Z`。
]

把这些归约串起来，就得到一条很有用的链：

`3-SAT <=_p Independent Set <=_p Vertex Cover <=_p Set Cover`。

这说明一旦把一个已知困难问题接到链上，后面的问题都会继承它的困难。

== 为什么复杂性理论偏爱 decision problem

很多现实任务看起来明明是“找最优解”，例如最小点覆盖、最短旅行路线、最大利润项目选择。
但课程在复杂性章节几乎总把它们写成 yes/no 问题。原因是：

- decision 形式更便于定义类 `P`、`NP`、`co-NP`；
- 而很多优化问题都可以自归约到它的判定版本。

点覆盖可以用来说明 *self-reducibility*：

1. 先通过二分答案，调用 decision oracle，找到最优值 $k^*$；
2. 再逐个尝试顶点 $v$：
  如果删去 $v$ 后的图仍存在大小至多 $k^*-1$ 的点覆盖，
  就说明某个最优解里可以包含 $v$；
3. 把 $v$ 放进答案，递归处理更小图。

#remark(title: "这说明了什么")[
  复杂性理论研究 decision version，并不是因为 search 或 optimization 不重要，而是因为它们常常可以通过多次调用 decision version 被恢复出来。
]

== P(polynomial)：有多项式时间算法的问题

#definition[
  `P` 是所有存在多项式时间算法的 decision problem 的集合。
]

你可以把 `P` 理解为课程中“目前知道怎样高效解决”的那一类问题。到现在为止，
我们学过的大量经典问题都在这里：

- 最短路；
- 最大流；
- 二分图匹配；
- 带权区间调度；
- 序列比对；
- 线性规划中的一些特例，等等。

例如 PRIMES：给定一个整数，判断它是不是素数。这个问题在 2002 年被证明属于 `P`。

== NP(nondeterministic polynomial)：答案容易验证的问题

#definition[
  若存在一个多项式时间算法 $C(s, t)$，使得对于任意实例 $s$，
  `s` 属于问题 $X$ 当且仅当存在一个长度至多为多项式的证书 $t$，
  让 `C(s, t)` 输出 yes，
  则称 $C$ 为 $X$ 的 certifier，而 $X$ 属于 `NP`。
]

这里的重点是别人给了你一个候选答案时，你能不能很快验出来。

三个标准例子：
- *COMPOSITES*：证书是一个非平凡因子；
- *SAT*：证书是一组布尔变量赋值；
- *HAM-CYCLE*：证书是一个顶点排列，表示 Hamiltonian cycle 的访问顺序。

所以 `NP` 的直觉是：*求解* 可能难；但 *验解* 很快。

#tip-block(title: "别把 NP 理解成“非多项式”")[
  `NP` 里的 `N` 是 nondeterministic，不是 non-polynomial。$P subset.eq "NP"$
]

== P、NP、EXP 之间的关系

先记两条最基本的包含关系：

#theorem(title: "复杂性类的基本包含")[
  `P subset.eq NP subset.eq EXP`。
]

理由分别是：

- `P subset.eq NP`：若一个问题本来就能直接多项式时间求解，那么把证书取成空串即可；
- `NP subset.eq EXP`：若一个问题有多项式长度证书，就可以暴力枚举所有可能证书，虽然数量是指数级，但终究还能在指数时间内完成。

这条链说明：

- `NP` 不代表不可解；
- 它只是介于“已知高效可解”和“暴力总能解”的中间地带。

== P versus NP：求解和验证是否一样难

最后来到这个部分的总问题：

#theorem(title: "P versus NP")[
  我们是否有 `P = NP`？
]

它可以翻译成一句非常朴素的话：

- 一个答案若容易验证；
- 是否也一定容易找到？

如果 `P = NP`，那么像 SAT、3-COLOR、TSP、Hamiltonian cycle 这类问题都将出现高效算法；
如果 `P != NP`，那就意味着存在一大类问题，其答案能迅速验对，但无法迅速求出。

#important-block(title: "为什么这个问题重要")[
  它不只决定几个教材题有没有快算法。
  它关系到密码学、自动验证、组合优化、机器学习中的离散搜索，
  也关系到“找到一个解”和“检查一个解”之间到底有没有本质差距。
]

== 本章小结

#figure(
  caption: [归约、P 与 NP 的主线],
  three-line-table[
    | 概念 | 要问的问题 | 记忆点 |
    |:---|:---|:---|
    | 多项式归约 | 会解 `Y` 能否带出 `X` | 读方向：`X <=_p Y` 表示 `Y` 至少和 `X` 一样难 |
    | 简单等价 / 特例 / gadget | 归约如何构造 | 不背图，抓强制出来的逻辑 |
    | `P` | 能否直接高效求解 | 有多项式时间算法 |
    | `NP` | 能否高效验证 yes 实例 | 有多项式长度证书和 certifier |
    | `P ?= NP` | 求解与验证是否同样容易 | 复杂性理论的中心问题 |
  ],
)

#tip-block(title: "读完这一章先检查这些")[
  - 正确读取归约方向；
  - 解释为什么点覆盖和独立集其实是同一结构的两面；
  - 理解 `3-SAT <=_p Independent Set` 在“强制什么逻辑”；
  - 用 certifier 的语言说清 `NP` 的定义；
  - 解释 `P subset.eq NP subset.eq EXP` 为何成立。
]
