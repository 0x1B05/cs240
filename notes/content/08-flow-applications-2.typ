#import "@local/notes:0.1.0": *

= 第八章 网络流应用二

#tip-block(title: "这一章的重点")[
  这一章不是再引入新理论，而是展示网络流建模的威力：
  某些问题表面上像体育排名、项目筛选、矿场开采，
  实际上都能化成 cut 与 flow。
]

== Baseball Elimination

#definition[
  对一支目标球队 $z$，已知每支球队当前胜场数 $w_x$，并记两支球队 $x, y$ 之间的剩余比赛数为 `g_xy`。问题是：是否存在一种剩余比赛结果，使得 $z$ 最终至少并列第一？
]

最简单的淘汰条件是：

- 若某队当前胜场已经大于 $z$ 最多可能达到的胜场，
  那么 $z$ 立刻被淘汰。

但这只是充分条件，不是必要条件。难点在于：即使每支单独球队看起来都没超过上限，它们之间剩下的比赛也会“制造新的胜场”，使得目标队仍然无望。

建模方法如下：

- 先假设目标队 $z$ 赢下自己所有剩余比赛；
- 对其他球队之间的每一场剩余比赛，建立一个“比赛节点”；
- 源点向每个比赛节点连边，容量等于这场比赛剩余次数；
- 比赛节点向对应两支球队连无穷容量边，表示这场比赛的胜场必须分给其中一队；
- 每支球队向汇点连边，容量是“它还能再赢多少而不超过 $z$ 的理论上限”。

于是：

#theorem(title: "Baseball Elimination 的最大流判定")[
  目标球队 $z$ 未被淘汰，当且仅当存在一个最大流，能够饱和所有从源点发出的边。
]

直观含义是：若所有剩余比赛都能被合法分配出去，且没有哪支球队超出允许上限，那么 $z$ 仍有机会；否则就被淘汰。

== 为什么最小割能给出“淘汰证书”

换一种面向体育记者的说法：若 $z$ 被淘汰，则存在一组球队，记作 `T*`。这些球队当前已经拿到的胜场，加上它们彼此之间剩余比赛必然产生的新增胜场，平均下来已经超过了 $z$ 的理论上限。

这组球队其实就来自最大流最小割中的源侧点集。

#important-block(title: "这很有代表性")[
  最小割不仅能告诉你“做不到”，还常常能告诉你“究竟是哪一组约束合起来把它卡死了”。
  这组卡死你的对象，就是一个证书。
]

== Project Selection

#definition[
  给定若干项目，每个项目有收益，记作 `p_v`，可以为正也可以为负。
  若 $(v, w)$ 是先修关系，则做项目 $v$ 之前必须同时做 $w$。
  目标是在满足全部先修关系的前提下，使总收益最大。
]

这是最小割建模里非常经典的一类：

- 若某项目收益为正，则从源点向它连一条容量等于该收益的边；
- 若某项目收益为负，则从该项目向汇点连一条容量等于其成本绝对值的边；
- 每条先修边 $(v, w)$ 赋无穷容量，表示“若选了 $v$ 却不选 $w$，切割代价将无法接受”。

此时任意一个有限容量 cut 的源侧项目集合，必然满足先修约束；而最小割对应的源侧项目集合，正好给出最大净收益方案。

#remark(title: "为什么正收益连源点、负收益连汇点")[
  你可以把 cut 理解成“把哪些项目留在源侧、哪些扔到汇侧”。
  把一个正收益项目扔掉，会损失它的收益；
  把一个负收益项目留下，则要承担它的成本。
  最小割正是在平衡这两部分代价。
]

== Open Pit Mining

露天采矿问题是 project selection 的一个几乎直接的实例化：

- 每个土块有净价值；
- 某块矿石若位于另一块之下，上层土块就构成它的先修条件；
- 目标是决定挖哪些块，使总净收益最大。

因此：

- 每个土块是一个“项目”；
- 地质依赖关系是先修边；
- 收益与成本直接转成源边或汇边。

这也说明 project selection 并不是抽象玩具，而是真正有工程背景的优化模型。

== 算法范式阶段小结

到网络流应用为止，课程已经串起了几条非常重要的算法设计主线：

#figure(
  caption: [课程主要算法范式总览],
  three-line-table[
    | 范式 | 典型问题 | 关键问题 |
    |:---|:---|:---|
    | 贪心 | 区间调度、最小迟到、MST | 局部选择为何安全 |
    | 分治 | 最近点对、Karatsuba、FFT | 子问题如何拆，合并如何快 |
    | 动态规划 | 带权区间调度、背包、序列比对 | 状态如何定义，重复如何消除 |
    | 网络流 | 匹配、割、项目选择、体育淘汰 | 如何把约束翻译成容量与守恒 |
  ],
)

#tip-block(title: "学完这门课后最该具备的能力")[
  当你拿到一个新问题时，不要先问“能不能套某个模板”，而应先问：

  - 这个问题的结构更像局部决策，还是全局状态？
  - 子问题是否独立，还是会重复？
  - 是否存在一种 cut、flow、matching 或 interval 的自然建模？
  - 我需要证明的是正确性、复杂度，还是两者都要？
]

== 算法范式复习建议

如果你要考前快速复习，我建议按下面顺序回顾：

1. 先掌握四种范式的“适用信号”。
2. 再熟悉每种范式 2 到 3 个代表题。
3. 然后单独练习证明套路：
   exchange argument、stays ahead、状态递推、最大流最小割。
4. 最后回到建模题，练习把文字约束翻译成图结构。

#important-block(title: "最后一句话")[
  算法设计真正训练的，是 *把问题看成结构* 的能力。
  当你能把一个具体问题抽象成 interval、state、cut、matching 或 residual graph 时，
  解法往往已经出现了一半。
]
