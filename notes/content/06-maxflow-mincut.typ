#import "@preview/theorion:0.4.1": *
#import "@preview/tablem:0.3.0": three-line-table

= 第六章 最大流最小割

#tip-box(title: "这一章的目标")[
  这一章不是再教一个算法，而是回答 Ford-Fulkerson 最关键的问题：
  *什么时候我们可以确信当前流已经最优？*
]

== cut 是什么，为什么它天然给出上界

#definition[
  一个 $s$-$t$ cut 是把顶点集划分成 $(A, B)$，其中 $s in A$、$t in B$。
  该 cut 的容量是所有从 $A$ 指向 $B$ 的边容量之和，记为 `cap(A, B)`。
]

直觉上，任何从 $s$ 流向 $t$ 的流，最终都必须跨越每一个 $s$-$t$ cut。因此某个 cut 的容量，天然是最大流的上界。

先看 *flow value lemma*，本质是：

- 看任意一个 cut；
- 净穿过这个 cut 的流量，恰好等于从源点流出的总量，也就是流值。

于是立刻得到：

#theorem(title: "弱对偶")[
  对任意流 $f$ 和任意 $s$-$t$ cut $(A, B)$，都有
  `v(f) <= cap(A, B)`。
]

这句话非常重要，因为它说明了：最大流问题和最小割问题天然互为上下界。

== 最大流最小割定理

#theorem(title: "Max-Flow Min-Cut Theorem")[
  任意流网络中，最大流的值等于最小割的容量。
]

证明会把下面三个条件证明为等价：

1. 存在某个 cut，使得流值等于该 cut 容量；
2. 当前流是最大流；
3. 当前残量图中不存在增广路。

这里最值得记住的是 `(iii) -> (i)` 这一步：

- 若没有增广路，就把残量图中所有从 $s$ 可达的点收集成集合 $A$；
- 其余点为 $B$；
- 因为 $t$ 不可达，所以这是一个合法 cut；
- 所有从 $A$ 到 $B$ 的边都已饱和；
- 所有从 $B$ 到 $A$ 的边都没有正流可退；
- 因而当前流值正好等于这个 cut 的容量。

这一下同时说明了两件事：

- 当前流确实最优；
- 你还顺手拿到了一个最小割作为证书。

#important-box(title: "考试时最该会复述的一段")[
  “没有增广路”并不只是算法停了，它意味着残量图把点集分成了
  ‘从源点还能到’ 与 ‘再也到不了’ 两部分。
  这条边界正好构成一个被当前流完全填满的 cut。
]

== 整数容量与可积性

接着是 *integrality theorem*：

#theorem(title: "可积性定理")[
  若所有边容量都是整数，则存在一个所有边流量也都是整数的最大流。
]

这件事非常关键，因为后面很多“组合优化”建模其实需要的是 0-1 选择，而不是任意实数流量。整数容量保证了最大流不会莫名其妙地产生半条边、半场比赛、半个匹配。

== 为什么一般的 Ford-Fulkerson 不是多项式

如果每次随便找一条增广路，那么：

- 每次流值至少增加 1；
- 若最大流值很大，迭代次数也可能很大；
- 复杂度会依赖于容量数值 $C$ 本身，而不是输入长度 `log C`。

因此普通 Ford-Fulkerson 的复杂度上界是 `O(mnC)`，不是严格意义上的多项式时间。

还有一个更尖锐的事实：

- 若容量是无理数，算法甚至可能不终止。

#warning-box(title: "别把‘会增广’误当成‘会高效增广’")[
  最大流问题的难点不只是找到一条增广路，而是要 *聪明地* 选择增广路，
  否则算法虽然正确，效率却可能很差。
]

== 容量缩放：让增广更有价值

一种常见改进策略是 *capacity scaling*。

它维护一个尺度参数 `Delta`，只在残量图中保留容量至少为 `Delta` 的边，形成
`Delta-residual graph`。于是每次增广都有较大的瓶颈：

- 外层循环不断把 `Delta` 减半；
- 每个阶段只处理“大于当前阈值”的增广机会；
- 当 `Delta = 1` 且再无增广路时，算法结束。

分析结论是：

- 增广次数为 `O(m log C)`；
- 可实现为总时间 `O(m^2 log C)`。

这比朴素 Ford-Fulkerson 更接近真正的多项式算法。

#remark(title: "容量缩放的直觉")[
  先把大流量快速送过去，再处理细枝末节。
  这和数值算法中“先解决大误差，再修小误差”的思想很像。
]

== 本章小结

#figure(
  caption: [最大流定理链条],
  three-line-table[
    | 概念 | 作用 | 结果 |
    |:---|:---|:---|
    | Cut | 给出所有流的上界 | `v(f) <= cap(A, B)` |
    | 残量图 | 描述还能怎么调整当前流 | 增广路存在则还能改进 |
    | 无增广路 | 诱导出一个特殊 cut | 当前流值等于该 cut 容量 |
    | 整数容量 | 保证整数最大流存在 | 便于组合优化建模 |
  ],
)

#tip-box(title: "你必须会的一句话")[
  *最大流最小割定理* 不是孤立结论，而是“流、残量图、增广路、割”四个概念被同一条结构链条串起来的结果。
]
