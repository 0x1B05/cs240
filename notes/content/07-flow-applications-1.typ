#import "@preview/theorion:0.4.1": *
#import "@preview/tablem:0.3.0": three-line-table

= 第七章 网络流应用一

#tip-box(title: "学网络流应用的重点")[
  不要只记住“这题能用最大流”，而要学会把现实约束翻译成：
  容量、守恒、上下界、源点、汇点、无限边。
]

== 二分图匹配

#definition[
  在一个二分图中，一个 matching 是一组边，
  使每个顶点至多出现在其中一条边里。目标是求最大匹配。
]

最大流建模是网络流中最经典也最重要的套路之一：

- 新建源点 $s$ 和汇点 $t$；
- 从 $s$ 向每个左侧点连容量为 1 的边；
- 每条二分图边从左指向右，容量设为 1；
- 每个右侧点向 $t$ 连容量为 1 的边。

这样一来：

- 流过一条左到右的边，就对应选中一条匹配边；
- 左右两边每个点流量最多为 1，就保证没有冲突。

#theorem(title: "二分图最大匹配与最大流等价")[
  二分图中的最大匹配大小，等于上述流网络中的最大流值。
]

证明的两边都很直接：

- 从匹配到流：每条匹配边送 1 单位流；
- 从流到匹配：利用整数流性质，所有左到右的流都可看成 0-1 选择。

== 完美匹配与 Hall 定理

#definition[
  完美匹配指每个顶点都恰好被匹配一次。
]

接着讨论一个自然问题：什么时候二分图存在完美匹配？

这时出现著名的 *Marriage Theorem*：

#theorem(title: "Hall 定理")[
  对任意左右点数相同的二分图，存在完美匹配，当且仅当对左侧任意子集 `S`，它在右侧的邻居数都不少于 `S` 自身的大小。
]

这一定理的含义很直观：左侧任意一批人，右侧可供匹配的邻居至少要和它一样多，否则必然不够分。

可以用最小割证明：若不存在完美匹配，则最大流不满；由某个最小割可反推出一组违反 Hall 条件的顶点集合。

#important-box(title: "Hall 条件的地位")[
  这是“局部可行性条件推出全局可行匹配”的经典例子。
  它也是很多匹配存在性判定的原型。
]

== 边不相交路径

如果图中每条边容量都设为 1，那么最大流值等于最多能找到多少条两两边不相交的 $s$-$t$ 路径。

这件事背后的逻辑也很自然：

- 一条路径就是 1 单位流；
- 边容量为 1 保证不同路径不能共用同一条边；
- 整数流又保证最大流可以拆成若干条路径。

所以 *edge-disjoint paths* 本质上只是最大流的另一个表述。

== 有供需的 circulation

网络流还可以进一步推广到 *circulation with demands*。

与普通最大流不同，这里不再只有一个源点和一个汇点，而是：

- 某些点需要净流入，叫 demand；
- 某些点需要净流出，叫 supply；
- 中间点继续满足守恒。

建模方法是：

- 加一个超级源点 $s$；
- 对所有 supply 点，从 $s$ 向它连边；
- 加一个超级汇点 $t$；
- 对所有 demand 点，向 $t$ 连边；
- 检查是否能把从超级源点发出的流全部送满。

这一步非常值得掌握，因为很多“分配是否可行”的问题，其实都不是在求一个最大值，而是在问 *存在不存在一个满足全部约束的流*。

== 带下界的 circulation

有时边上不只是上界 $c(e)$，还会要求
`l(e) <= f(e) <= c(e)`。

处理思路是先把每条边的下界流量“预先送掉”，再把它对端点供需造成的影响记到账上，转化成一个普通的 circulation with demands 问题。

#tip-box(title: "遇到下界时怎么想")[
  下界不是新障碍，而是“已经被强制送出的那部分流”。
  先把它扣掉，剩下的自由度才是你真正需要优化或判定的对象。
]

#remark(title: "真正落到纸面上的变换")[
  若边 $e = (u, v)$ 的下界是 $l(e)$、上界是 $c(e)$，就先令
  `f'(e) = f(e) - l(e)`，
  于是新流量只需满足
  `0 <= f'(e) <= c(e) - l(e)`。

  但这意味着原本被强制送出的 $l(e)$ 单位流，已经让：

  - 顶点 $u$ 额外“送出”了 $l(e)$；
  - 顶点 $v$ 额外“收到”了 $l(e)$。

  若用 `b(v) = 净流入需求` 来记账，那么剩余网络中：

  - `u` 还需要弥补这次强制流造成的净流出，所以 `b(u)` 增加 $l(e)$；
  - `v` 已经提前收到 $l(e)$ 单位流，所以 `b(v)` 减少 $l(e)$。

  全部边都这样处理完后，剩下的就是一个普通的 circulation with demands 可行性问题。
]

== Survey Design

问卷设计问题是 circulation with lower bounds 的一个典型应用：

- 消费者与产品之间是否能提问，由一条边表示；
- 每个消费者被问的问题数有上下界；
- 每个产品被多少消费者评价也有上下界。

只要把这些条件翻译成边容量与下界，问题就变成：

- 是否存在一个整数 circulation；
- 若存在，对应的流量就是一份可行问卷方案。

== 图像分割与最小割

另一个很经典的应用是 foreground/background segmentation。

对每个像素 $i$：

- $a_i$ 表示它属于前景的倾向；
- $b_i$ 表示它属于背景的倾向；
- 邻接像素之间还有一个 separation penalty，惩罚相邻像素被分到不同类别。

建模时：

- 源点代表前景；
- 汇点代表背景；
- 像素连向源点和汇点的边，表达它更像前景还是背景；
- 像素之间的边表达“相邻像素最好别被切开”的代价。

这样，求最优前景/背景划分就变成求一个最小割。

#remark(title: "网络流为什么适合做视觉问题")[
  因为“像素分类”既要照顾单点偏好，又要照顾邻域平滑性。
  最小割恰好天然表达“切开一条边要付代价”。
]

== 本章小结

#figure(
  caption: [网络流应用中的常见建模套路],
  three-line-table[
    | 现实含义 | 图中元素 | 约束含义 |
    |:---|:---|:---|
    | 一对一分配 | 容量为 1 的边 | 一个对象至多匹配一次 |
    | 多条独立通路 | 单位容量 | 每条边只能被一条路占用 |
    | 供需平衡 | circulation | 净流入/净流出满足指定值 |
    | 平滑分割 | 最小割 | 切开相邻元素要付代价 |
  ],
)

#tip-box(title: "你该养成的建模习惯")[
  看到一个新问题时，先问：
  “这里谁在被分配？谁在竞争同一资源？哪些关系必须同时成立？
  有没有一种流量守恒或 cut 代价可以把这些约束统一描述？”
]
