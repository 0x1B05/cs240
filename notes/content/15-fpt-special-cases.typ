#import "@preview/theorion:0.4.1": *
#import "@preview/tablem:0.3.0": three-line-table

= 第十五章 FPT：NP-hard 以后还能怎么做

学完 NP-complete 以后，很容易产生一个误解：既然问题难，那就没有算法可讲了。这一讲正是在纠正这个误解。

理论说的是：对任意输入，想要精确多项式时间算法，大概率没有希望。但实际问题常常不是“任意输入”。有时解很小，有时图长得像树，有时某个局部深度很小。抓住这些额外结构，就能重新做出好算法。

== 三个愿望，至少放弃一个

对 NP-complete 问题，我们通常同时想要三件事：

- 多项式时间；
- 精确最优；
- 适用于所有输入。

一般情形下三者不能全要。这一讲选择放弃第三个愿望：不处理所有输入，而是处理带有小参数或特殊结构的输入。

== 什么是 FPT

#definition[
  若一个问题关于参数 $k$ 能在 `f(k) * poly(n)` 时间内解决，就称它关于 $k$ 是 *fixed-parameter tractable*，简称 `FPT`。
]

这个定义的重点是：指数部分只允许依赖 $k$，不能依赖完整输入规模 $n$。

比如：

- `n^k` 对 $n = 1000, k = 10$ 基本没法跑；
- `2^k * n` 在同样参数下就很现实。

所以 FPT 不是说“没有指数”，而是把指数限制在你希望很小的参数上。

== 小 vertex cover

先看 vertex cover。

#definition[
  给定图 $G = (V, E)$ 和整数 $k$，问是否存在点集 $S subset.eq V$，满足 `|S| <= k`，并且每条边至少有一个端点在 $S$ 中。
]

暴力枚举所有大小为 $k$ 的点集，大约是 $n^k$。当 $n = 1000$、$k = 10$ 时，这个数已经太大。

但 vertex cover 有一个简单到不能再简单的分支规则：

#theorem(title: "分支规则")[
  取任意一条边 $(u, v)$。任何 vertex cover 都必须包含 $u$ 或 $v$ 中至少一个。
]

于是我们递归：

- 把 $u$ 放进解里，删掉 $u$ 和它 incident 的边，预算变成 $k-1$；
- 或者把 $v$ 放进解里，做同样的事。

如果图已经没有边，说明覆盖成功。若 $k$ 用完但还有边，就失败。

递归深度最多 $k$，每层两个分支，所以只有 `O(2^k)` 个递归节点。每个节点里找边、删边、检查都可以做成多项式时间，因此总时间是 `O(2^k * poly(n))`。如果实现得比较仔细，可以写成更简化的 `O(2^k n)`；重点是指数只落在 $k$ 上。

#remark(title: "这比暴力枚举强在哪里")[
  暴力枚举是在问：“从 $n$ 个点里挑哪 $k$ 个？”
  分支算法是在问：“这条还没覆盖的边，必须由哪一端来覆盖？”
  后者每一步都让预算 $k$ 下降，因此搜索树只按 $k$ 展开。
]

== 递归树复习：别只背 Master theorem

这里回顾 mergesort 和 Master theorem，是为了提醒你分析递归时先画树。

mergesort 的树是：

- 第 0 层 1 个规模 $n$ 的问题；
- 第 1 层 2 个规模 $n/2$ 的问题；
- 每层总工作量都是 $n$；
- 一共 `log n` 层。

所以总时间是 `Theta(n log n)`。

小 vertex cover 的树不一样：

- 每层分支数翻倍；
- 深度不是 `log n`，而是 $k$；
- 节点总数是 `1 + 2 + 4 + ... + 2^k`。

这就是 FPT 分析经常出现的形状：指数树还在，但树高由小参数控制。

== 树上的独立集

一般图上的 maximum independent set 是 NP-hard。但如果图是一棵树，结构突然变得很友好。

#definition[
  树上的 independent set：给定一棵树，选尽量多的点，使任意两个被选点之间没有边。
]

无权版本有一个很漂亮的事实：

#theorem(title: "叶子可以被安全选择")[
  若 $v$ 是叶子，$u$ 是它唯一的邻居，则存在一个最大独立集包含 $v$。
]

证明可以自己顺一遍：

- 如果某个最大独立集已经选了 $v$，不用动；
- 如果它既没选 $u$ 也没选 $v$，那加上 $v$ 会更大，矛盾；
- 如果它选了 $u$ 没选 $v$，把 $u$ 换成 $v$，大小不变，而且仍然独立。

所以算法可以反复选叶子：

1. 找一条边 $(u, v)$，其中 $v$ 是叶子；
2. 把 $v$ 加入答案；
3. 删除 $u$ 和 $v$，以及相关边；
4. 最后把剩余孤立点加入答案。

树上这样做是对的，因为叶子和它的邻居形成了一个很局部的选择。

== 带权版本：贪心不够了

如果每个点有权重，选叶子就不一定对。一个叶子权重可能很小，而它的父节点权重很大。这里要换成树形 DP。

任意选一个根 $r$。对每个节点 $u$ 定义两个值：

- `OPT_in(u)`：在 $u$ 的子树中，要求选 $u$ 时的最大权重；
- `OPT_out(u)`：在 $u$ 的子树中，要求不选 $u$ 时的最大权重。

递推很自然：

- 选 $u$，孩子都不能选：
  `OPT_in(u) = w_u + sum OPT_out(v)`；
- 不选 $u$，每个孩子自己决定选不选：
  `OPT_out(u) = sum max(OPT_in(v), OPT_out(v))`。

按 postorder 从叶子算到根，最后答案是：

`max(OPT_in(r), OPT_out(r))`

时间 `O(n)`。这里可以看到树的价值：一旦父节点状态固定，不同子树之间就互不影响。

== bounded treewidth 的直觉

这里先不要求你掌握 bounded tree width 的正式定义，把它理解成一句话就够了：

有些图虽然不是树，但“离树不远”。它们可以被拆成一些小块，小块之间通过少量顶点连接。只要连接处很小，DP 仍然能做，因为你只需要记住少量边界状态。

这就是很多 FPT 算法的来源：参数不一定是解的大小，也可以是图结构的复杂度。

== Circular Arc Coloring

最后一个例子来自光纤通信。多条通信路径可以共享光纤，但如果两条路径经过同一段边，它们必须用不同波长。若网络是一个环，每条路径就对应圆上的一段弧。

#definition[
  *Circular arc coloring*：给圆上的若干弧染色，使有冲突的弧颜色不同。问 $k$ 种颜色是否够用。
]

普通 interval coloring 很好做：把区间按起点排序，贪心就能用等于最大 depth 的颜色数完成。圆弧麻烦在于它绕成一圈，没有天然的左端点和右端点。最大 depth 仍然是下界，但不一定等于最优颜色数。

处理办法是把圆切开。切开后：

- 没跨过切点的弧变成普通区间；
- 跨过切点的弧被切成两段；
- 这两段必须使用同一种颜色。

于是问题变成线上的动态规划。沿着节点 $v_0, v_1, ..., v_n$ 扫过去，状态记录“当前穿过这个位置的弧分别是什么颜色”。如果 depth 不超过 $k$，同一位置最多只有 $k$ 条弧穿过，所以可能的颜色排列至多是 `k!`。

每一步只保留和前一步兼容的着色。扫到最后，再检查那些被切开的弧前后颜色是否一致。运行时间可以写成：

`k! * poly(m, n)`

当 $k$ 很小的时候，这就是一个可用的 FPT 算法。

== 本章小结

#figure(
  caption: [NP-hard 问题重新可做的几种入口],
  three-line-table[
    | 入口 | 本讲例子 | 你在题目里要找什么 |
    |:---|:---|:---|
    | 解很小 | 小 vertex cover | 参数 $k$ 小，分支时让 $k$ 下降 |
    | 图像树 | 树上独立集 | 子问题被分开，适合 DP |
    | 局部拥挤度小 | circular arc coloring | 状态数由 depth 或颜色数控制 |
  ],
)

#tip-box(title: "读题时可以这样问")[
  这个问题一般情形 NP-hard，但我的输入有没有小参数？
  图是不是接近树？
  同时发生冲突的对象是不是很少？
  如果答案是 yes，就不要急着放弃精确算法。
]
