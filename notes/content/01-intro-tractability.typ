#import "@preview/theorion:0.4.1": *
#import "@preview/tablem:0.3.0": three-line-table

= 第一章 课程导论与复杂性视角

#tip-box(title: "这门课真正想训练的")[
  1. 看出一个问题的结构；
  2. 选择合适的算法范式；
  3. 用严谨分析说明它为什么快、为什么对。
]

== 课程在讲什么

你已经学过排序、搜索、分治、图算法这些基础内容；本课程更进一步，关心的是：
- 如何从问题结构中提炼算法。
- 如何判断一个想法是不是只“看起来合理”。
- 如何分析增长阶，而不是只看样例跑得快不快。
- 如何区分“能做”和“能高效地做”。

#definition[
  *算法* 是一个有限、明确、可执行的过程：给定输入，经过一系列规则化步骤，产生输出。
]

课程一开始列了大量应用背景：互联网、编译器、数据库、生物信息、图形学、推荐系统、图像处理、物理模拟等。结论很简单：算法不是某个小方向的技巧，而是整个计算机学科的共通语言。

== 为什么分析运行时间

写出一个正确程序只是第一步。真正困难的是：当输入规模变大时，它还能不能工作？

归并排序是第一个适合拿来练习分析的例子。它说明“设计”和“分析”是绑在一起的：
- 设计上，把数组分成两半。
- 递归地分别排好序。
- 线性时间合并两个有序数组。

于是得到递归式 `T(n) = 2T(n / 2) + O(n)`。

递归树告诉我们：每一层总工作量都是线性的，而层数是 `log n`，所以总时间是 `O(n log n)`。这就是本课程的一个基本口味：不要只会写程序，还要能解释为什么增长阶是这个数量级。

#important-box(title: "本课默认分析口径")[
  除非特别说明，本课优先讨论 *最坏情况复杂度* 与 *渐近增长阶*。原因不是它唯一正确，而是它最稳定、最通用，也最容易支持严谨证明。
]

== 三条分析原则

Lecture 0 里有三条很重要的分析原则，可以直接作为做题时的默认思路：
1. 优先看最坏情况。因为我们想要的是“任何输入都成立”的保证。
2. 不过分纠结常数项和低阶项。它们会受机器、编译器、实现细节影响。
3. 用渐近分析看大规模输入。因为真正有价值的问题，往往都是规模一大，差距就显现出来。

这三条合起来，形成了整个课程的分析风格：我们不是完全忽略实践，而是先建立一个不会被细节噪声淹没的理论骨架。

== 多项式时间与“高效”

Lecture 1 的核心问题是：什么叫“可计算”，什么又叫“可高效计算”？

#definition[
  若把输入规模记为 $n$，并且一个算法的运行步数始终不超过某个多项式，例如 `a * n^b`，则称该算法为 *多项式时间算法*。
]

课程把“高效”近似等同于“多项式时间”，原因有两个：
- 理论上，多项式时间对计算模型相对鲁棒，不会因为底层机器细节轻易改变结论。
- 实践上，真正有用的多项式算法通常也会伴随较小的指数和较合理的常数。

当然，不要把“多项式”神化。有些多项式算法指数极高，实践上仍然不可用；但它依旧是一个关键分界线，因为它把“指数爆炸”与“结构化求解”区分开了。

#warning-box(title: "不要机械地把多项式时间等于好算法")[
  `O(n^120)` 在理论上属于多项式时间，但几乎没有实践意义。课程使用“多项式时间”作为效率基线，是为了建立复杂性层面的分界，而不是说所有多项式算法都值得真正部署。
]

== 暴力搜索为什么常常不行

很多问题都有一个“自然但很差”的解法：枚举所有候选解，再逐个检查。例如：
- 稳定匹配可以暴力枚举所有完美匹配。
- 独立集可以暴力枚举所有点集。
- 若一个问题有 $2^n$、$n!$ 量级的候选解，通常就已经非常危险。

所以，算法设计的真正价值，常常在于找到一个能够打破暴力枚举的结构。换句话说：
- 分治在于发现递归分解结构；
- 贪心在于发现局部选择与全局最优的一致性；
- 动态规划在于发现重叠子问题和最优子结构；
- 网络流在于发现一个看似离散的问题，其实可以被“容量 + 守恒”统一建模。

== 渐近记号要会用，而不是只会背

Lecture 1 系统回顾了 `O`、`Omega`、`Theta` 记号。这里真正要学会的不是定义本身，而是它们分别在说什么：

- `T(n) = O(f(n))`：上界，表示“至多和 `f(n)` 一样快地增长”。
- `T(n) = Omega(f(n))`：下界，表示“至少和 `f(n)` 一样快地增长”。
- `T(n) = Theta(f(n))`：同阶，表示上下界一致。

#remark(title: "如何快速判断增长阶")[
  当你面对多项式加和时，通常是最高次项主导；当你面对若干个正函数相加时，常常有`f(n) + g(n) = Theta(max(f(n), g(n)))`。
  真正需要小心的是：对数、指数、阶乘这几类函数之间的增长速度差距非常大。
]

#figure(
  caption: [常见增长阶的直觉排序],
  three-line-table[
    | 级别 | 典型形式 | 直觉 |
    |:---|:---|:---|
    | 常数/对数 | `O(1), O(log n)` | 增长极慢，通常非常理想 |
    | 线性/线性对数 | `O(n), O(n log n)` | 大多数经典高效算法所在区间 |
    | 二次/三次 | `O(n^2), O(n^3)` | 小规模可接受，大规模需谨慎 |
    | 指数及以上 | `O(2^n), O(n!)` | 常常意味着暴力或接近暴力 |
  ],
)

== 五个代表性问题

Lecture 0 用五个问题做了课程预告，这一页非常值得记住，因为它揭示了“问题长得像，复杂性可能完全不同”：

#figure(
  caption: [五个代表性问题与课程想传达的信息],
  three-line-table[
    | 问题 | 目标 | 典型结论 |
    |:---|:---|:---|
    | Interval Scheduling | 选最多个互不重叠区间 | 贪心，`O(n log n)` |
    | Weighted Interval Scheduling | 选总权重最大的互不重叠区间 | 动态规划，`O(n log n)` |
    | Bipartite Matching | 选最大匹配 | 最大流建模 |
    | Independent Set | 选最大独立集 | 一般图上是 NP-complete |
    | Competitive Facility Location | 博弈式选点 | 复杂性更高，PSPACE-complete |
  ],
)

这组对比在提醒你：不能只凭“问题描述差不多”就猜算法。真正决定难度的是结构，而不是表面语义。

== 本章你应该掌握什么

#tip-box(title: "复习清单")[
  读完这一章，你应该能回答：

  - 为什么课程偏爱最坏情况和渐近分析？
  - 为什么多项式时间被视为效率基线？
  - 归并排序为什么是 `O(n log n)`？
  - 为什么暴力搜索通常意味着指数复杂度？
  - 为什么两个看起来相近的问题，可能落在完全不同的复杂性等级上？
]
