#import "@local/notes:0.1.0": *

= 第十四章 PSPACE：当“证书”变成一整套策略

这一讲最好先带着一个问题读：前面讲 NP 的时候，我们一直在说“如果答案是 yes，给我一个短证书，我能很快检查”。可很多问题根本不像是在找一张证书。比如两个人轮流玩游戏，先手是否必赢？这时你要证明的不是“有一个动作好”，而是“无论对手怎么回应，我都还有办法继续应对”。

这就是 PSPACE 登场的原因。它处理的不是更花哨的 SAT，而是那种需要在一棵很大的决策树里推演的问题。

== 从 Geography Game 看出新味道

Geography 游戏的文字版是这样的：Amy 先说一个首都，Bob 接着说一个以前一个首都末尾字母开头的首都，如此反复，谁接不上谁输。

图论版更干净：

#definition[
  *Geography on graphs*：给定有向图 $G = (V, E)$ 和起点 $s$。两个玩家轮流从当前点沿一条有向边走到一个没访问过的点。走不了的人输。问题是：先手有没有必胜策略？
]

这里的难点和 Hamiltonian path 不一样。Hamiltonian path 问“有没有一条好路径”；Geography 问“我第一步走哪里，才能保证你无论怎么接，我最后都赢”。这句话里有一层层交替：

- 我存在一个选择；
- 对手的所有回应都要考虑；
- 我还要对每个回应再找选择。

看到这种“我选、你选、我再选”的结构，就要意识到它已经不像普通 NP 题了。

== PSPACE 到底限制什么

#definition[
  `PSPACE` 是所有能用多项式空间解决的 decision problem。
]

这里限制的是内存，不是运行时间。算法可以很慢，只要它不用指数级空间，就仍然可能属于 PSPACE。

两个包含关系先记住：

#theorem[
  `P subset.eq PSPACE`，并且 `NP subset.eq PSPACE`。
]

第一条很直观：一个多项式时间算法最多写出多项式多的东西，所以空间也不会超过多项式。

第二条可以用 3-SAT 想。你可以枚举全部 $2^n$ 个赋值，每次只保存当前赋值并检查公式。时间很大，但内存只需要保存一个赋值、一个计数器和公式本身。对一般 NP 问题也是类似：证书可能很多，但可以一个一个试，不必同时全存下来。

#remark(title: "先把时间和空间分开")[
  很多同学第一次见 PSPACE 会困惑：指数时间不是已经很糟了吗？
  是的，它很糟，但复杂性分类在这里问的是另一个问题：如果允许慢，内存能不能省下来。
]

== QSAT：SAT 加上“对手”

普通 SAT 的形式是：

`exists x_1, x_2, ..., x_n，使 Phi(x_1, ..., x_n) 为真`

QSAT 则允许量词交替：

`exists X_1 forall X_2 exists X_3 ... Phi(X_1, X_2, X_3, ...)`

#definition[
  *QSAT*：给定一个带有存在量词和全称量词的布尔公式，判断整个公式是否为真。
]

把它当成游戏就很好懂：

- Amy 负责存在量词变量；
- Bob 负责全称量词变量；
- Amy 想让最后的公式为真；
- Bob 想破坏她。

所以 `exists` 不是在说“有一个固定答案”，而是在说“轮到 Amy 时，她可以选一个值”。`forall` 不是抽象符号，而是在说“Bob 可能选任意值，你都得能处理”。

== QSAT 为什么用多项式空间就够

最直接的算法就是递归求值。

遇到 `exists X_i`：

- 试 `X_i = true`；
- 再试 `X_i = false`；
- 只要有一个分支能让后面为真，就返回 true。

遇到 `forall X_i`：

- 两个分支都要检查；
- 只有两个分支都为真，才返回 true。

递归树有指数级大小，但程序运行时只沿着一条分支往下走。回溯时，当前分支的信息就可以丢掉。因此同时存在内存里的东西只是递归栈、当前部分赋值和公式。空间是多项式的。

#theorem(title: "QSAT in PSPACE")[
  QSAT 可以用多项式空间解决。
]

学到这里时要小心：我们并没有说 QSAT 快。我们只是说，它不需要指数级内存。

== Planning：状态很多，但不一定要全部存

planning problem 可以想成 8-puzzle、15-puzzle、魔方或物流调度的抽象版本。

#definition[
  *PLANNING*：有一组条件 $C$，初始状态 $c_0$，目标状态 $c^*$，以及一组操作。每个操作有前置条件，并会让某些条件变真、某些条件变假。问题是：能否通过一串操作从 $c_0$ 到达 $c^*$？
]

如果把每个状态都画成点，就得到一个 configuration graph。条件有 $n$ 个时，状态可能有 $2^n$ 个。直接 BFS 可以判断可达性，但要存很多状态，空间会很大。

PSPACE 的思路是：不要真的把整张图存下来，只判断“从 $c_1$ 到 $c_2$ 是否存在长度不超过 $L$ 的路径”。

做法像这样：

- 枚举一个中间状态 $m$；
- 递归检查 $c_1$ 到 $m$ 是否能在 $L/2$ 步内到达；
- 再递归检查 $m$ 到 $c_2$ 是否能在 $L/2$ 步内到达。

枚举中间状态会很慢，但递归深度只有 `log L`。每层只保存当前两个端点、长度上界和正在枚举的中间状态，所以空间仍然可以控制在多项式内。

这类技巧的关键是“隐式图搜索”：状态图可以按需生成，不必完整展开。

== PSPACE-complete

和 NP-complete 一样，PSPACE 里也有“最难”的问题。

#definition[
  若问题 $Y$ 属于 `PSPACE`，并且任意 `X in PSPACE` 都能多项式归约到 $Y$，则称 $Y$ 为 *PSPACE-complete*。
]

基准问题是：

#theorem(title: "QSAT 是 PSPACE-complete")[
  QSAT 是 PSPACE-complete。
]

所以如果你想证明某个游戏或规划问题 PSPACE-complete，常见路线就是从 QSAT 归约。直觉上，这很自然：QSAT 的量词交替，正好对应游戏里的两名玩家轮流选择。

目前我们得到的复杂性层次可以先记成：

`P subset.eq NP subset.eq PSPACE subset.eq EXPTIME`

这里不要读成“我们已经知道每一步都严格变大”。严格性有很多仍然未知。课程里更重要的是学会判断题目的气味：短证书是 NP 的气味，策略树和长期规划是 PSPACE 的气味。

== Competitive Facility Location

最后的 competitive facility location 是一个例子，说明 QSAT 的交替选择怎样被塞进一个看似实际的博弈问题。

问题是：

- 图上每个点有正权重；
- 两个玩家轮流选点；
- 如果某点的邻居已经被选过，这个点就不能再选；
- 问第二个玩家能否保证至少拿到 $B$ 的收益。

它在 PSPACE 中，因为可以递归搜索所有合法走法。每一步只保存当前局面和轮到谁走，不需要把整棵博弈树存下来。

证明它 PSPACE-complete 时，可以从 QSAT 构造。读这个构造时可以抓住四个作用：

- literal 点表示变量取真或取假；
- 互补 literal 连边，防止同一变量两边都被选；
- 大权重让玩家被迫按变量顺序行动；
- clause 点给第二个玩家最后的补分机会。

如果交替赋值没有满足某个 clause，第二个玩家就能在最后拿到对应的 clause 点；如果公式被满足，这个补分机会就被封住。这样，公式真假被翻译成了第二个玩家能否保证收益。

== 本章小结

#figure(
  caption: [从 NP 到 PSPACE 的直觉变化],
  three-line-table[
    | 问题类型 | 典型问法 | 复杂性直觉 |
    |:---|:---|:---|
    | SAT / Hamiltonian cycle | 是否存在一个解 | 找短证书，属于 NP 语境 |
    | QSAT / 博弈 | 是否存在一套应对策略 | 量词交替，属于 PSPACE 语境 |
    | Planning | 是否有一长串操作可达目标 | 状态图巨大，但可隐式搜索 |
  ],
)

#tip-block(title: "读题时的检查点")[
  如果题目只让你交出一个候选解，先想 `NP`。
  如果题目要求你面对对手的所有回应，或者在指数状态空间里规划一条路，就要开始想 `PSPACE`。
]
