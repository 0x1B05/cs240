#import "@local/notes:0.1.0": *

= 第三章 分治算法

#tip-block(title: "这一章的主线")[
  分治算法的关键不是“写递归”，而是找到合适的拆分方式，让子问题足够独立、合并足够便宜，或者用代数技巧减少递归子问题个数。
]

== 从贪心转向分治

分治的标准模板是：
1. 把原问题分成若干更小的子问题；
2. 递归求解子问题；
3. 在线性或近线性的代价下合并答案。

如果每次都把规模为 $n$ 的问题分成两个规模约为 $n/2$ 的子问题，并且合并代价是 $O(n)$，通常会得到
$T(n) = 2T(n/2) + O(n) = Theta(n log n)$。

== 归并排序再看一遍

这里课程不是想再教你如何写 mergesort，而是想用它作为分治范式的标准样板：
- divide：数组对半切开；
- conquer：递归排序左右两半；
- combine：线性时间合并。

应该把 mergesort 当成“所有良好分治算法的参考模板”：分得干净、子问题独立、合并便宜。

== 最近点对：暴力到分治

#definition[
  *Closest Pair of Points*：给定平面上的 $n$ 个点，求欧氏距离最近的一对点。
]

暴力解法检查所有点对，需要 $Theta(n^2)$ 次比较。分治做法是：
- 按 $x$ 坐标把点分成左右两半；
- 递归求左边最近点对和右边最近点对；
- 设两侧答案较小值为 $delta$；
- 再检查是否存在跨越分界线、距离更小的点对。

真正的难点在 combine：

- 若一个跨边界点对距离小于 $delta$，那么两点必须都落在分界线两侧宽度为 $2 delta$ 的 strip 内。
- 把 strip 中的点按 $y$ 排序。
- 对每个点，只需和其后常数个点比较即可，常用上界是 11 个位置。

因此 combine 能在线性时间内完成，最终总复杂度是 $O(n log n)$。

#tip-block(title: "最近点对最关键的观察")[
  不是“只看 strip”这句话本身，而是：
  在 strip 里继续利用几何稀疏性，证明每个点只需要检查常数个候选。
  如果做不到这一步，分治依然会退化到二次复杂度。
]

=== 为什么要预排序两次

这里特别要注意：若每层递归都重新排序，复杂度会变差。标准做法是在递归前：
- 按 $x$ 坐标排一次；
- 按 $y$ 坐标再排一次；
- 递归时复用这两份顺序信息。

这样才能把 combine 的代价稳定在线性级别，从而保住 $O(n log n)$ 总复杂度。

== 整数乘法：从四次递归到三次递归

先看最普通的分治整数乘法。把两个 $n$ 位整数拆成高低两半：

`x = a * B^m + b`，`y = c * B^m + d`。

则 `xy = ac * B^(2m) + (ad + bc) * B^m + bd`。

若直接递归，需要计算：

- `ac`
- `ad`
- `bc`
- `bd`

于是递归式约为 `T(n) = 4T(n/2) + O(n)`，总复杂度仍是 `Theta(n^2)`。

Karatsuba 的突破在于：

- 先算 `ac`；
- 再算 `bd`；
- 再算 `(a+b)(c+d)`；
- 然后用
  `(a+b)(c+d) - ac - bd = ad + bc`
  还原中间项。

这样只需要 3 次递归乘法，于是：

`T(n) = 3T(n/2) + O(n) = O(n^(log_2 3)) ~= O(n^1.585)`。

#important-block(title: "Karatsuba 的本质")[
  Karatsuba 不是“算得更巧”，而是 *用额外的加减法换掉一次递归乘法*。
  在分治里，乘法子问题的数量通常比线性级别的加法更值得节省。
]

== 矩阵乘法：为什么 Strassen 值得学

普通 `n x n` 矩阵乘法的三重循环复杂度是 `Theta(n^3)`。若把矩阵按块切成四个 `(n/2) x (n/2)` 子矩阵，朴素分治需要 8 次递归子乘法，递归式是：`T(n) = 8T(n/2) + O(n^2)`，结果并没有变快，仍是 `Theta(n^3)`。

Strassen 的想法和 Karatsuba 非常像：用更多加减法，换更少的递归乘法。它证明了：

#theorem(title: "Strassen 乘法的关键结论")[
  两个 `2 x 2` 的分块矩阵相乘，只需要 7 次子矩阵乘法，而不是 8 次。
]

于是得到递归式
`T(n) = 7T(n/2) + O(n^2)`，
总复杂度降为
`O(n^(log_2 7)) ~= O(n^2.807)`。

同时注意两点：
- 理论上，后续还有更快的矩阵乘法上界。
- 实践上，越往后那些更优上界通常越不实用。

#remark(title: "为什么课程仍然强调 Strassen")[
  因为它是第一个非常清晰地展示“减少一次递归子问题，就能改变整体指数”的经典例子。它的教学价值往往大于工程价值。
]

== 多项式表示：系数形式与点值形式

分治的最后一个重要例子是 FFT。先不要急着看公式，先理解问题背景。

一个次数至多为 $n-1$ 的多项式，可以有两种常用表示方式：

- 系数表示：直接把每个幂次前的系数列出来；
- 点值表示：选取 $n$ 个互异点，并记录多项式在这些点上的取值。

这两种表示各有优劣：

- 系数表示下，加法方便、求值不够快；
- 点值表示下，多项式相乘很方便，因为只需逐点相乘。

所以真正的问题是：能不能高效地在这两种表示之间来回转换？

#figure(
  caption: [两种多项式表示的对比],
  three-line-table[
    | 表示方式 | 优点 | 缺点 |
    |:---|:---|:---|
    | 系数表示 | 加法直接，形式直观 | 在很多点上求值慢 |
    | 点值表示 | 逐点乘法很快 | 插值恢复系数较难 |
  ],
)

== FFT 的分治思想

FFT 的核心等式是把一个多项式按奇偶项拆开：`A(x) = A_even(x^2) + x A_odd(x^2)`。

这非常适合分治，因为：

- 原来是一个规模为 $n$ 的求值问题；
- 现在变成两个规模为 $n/2$ 的求值问题。

若我们选取的求值点具有特殊结构，使得平方以后仍然落在同一组点里，就能递归到底。这组最合适的点就是 *单位根*。

#definition[
  $n$ 次单位根是满足“某个复数的 $n$ 次方等于 1”的那组特殊点。FFT 选用它们，是因为这些点在平方后仍然保持很整齐的结构，便于把规模一分为二。
]

于是系数到点值的转换可以做到：`T(n) = 2T(n/2) + O(n) = O(n log n)`。

逆变换同理，也能在 `O(n log n)` 时间内完成。

== 用 FFT 做多项式乘法

若要计算两个多项式 $A(x)$ 和 $B(x)$ 的乘积：
1. 先把两者转换为点值表示；
2. 在每个点上直接相乘，得到 `C(x_i) = A(x_i) B(x_i)`；
3. 再做逆 FFT，恢复乘积多项式的系数。

因此，多项式乘法可以从朴素的 `Theta(n^2)` 降到 `O(n log n)`。

#tip-block(title: "FFT 里最值得记住的一件事")[
  不是每个公式细节，而是这个套路：选择一组“分治后仍保持闭合”的求值点，让大问题自然变成两个规模减半的小问题。
]

== 本章小结

#figure(
  caption: [分治问题中的典型加速结构],
  three-line-table[
    | 问题 | 朴素结构 | 分治改进点 |
    |:---|:---|:---|
    | 归并排序 | 直接排序整个数组 | 对半拆分，线性合并 |
    | 最近点对 | 检查所有点对 | strip 中只需检查常数个候选 |
    | 整数乘法 | 4 个半规模乘法 | 用加减法恢复交叉项，减少到 3 个 |
    | 矩阵乘法 | 8 个半规模矩阵乘法 | Strassen 用 7 个子乘法完成 |
    | FFT | 在很多点逐个求值 | 奇偶拆分 + 单位根闭合 |
  ],
)

#tip-block(title: "你应该形成的直觉")[
  一道分治题真正的亮点，往往不是“会递归”，而是：
  能否让合并步骤保持便宜，或者把原本看似不可避免的某个递归子问题省掉。
]
